apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsgenie-marid.fullname" . }}-test-connection"
  labels:
{{ include "opsgenie-marid.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "opsgenie-marid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
