apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dependency-track.fullname" . }}-test-frontend"
  labels:
{{ include "dependency-track.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dependency-track.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
