{{- if .Values.apiserver.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "dependency-track-apiserver.serviceAccountName" . }}
  labels:
{{ include "dependency-track.labels" . | indent 4 }}
{{- end -}}
